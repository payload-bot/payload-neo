version: '3.5'

services: 
    bot:
        build: .
        container_name: "payload-prod"
        restart: always
        env_file: 
            - ./.env
        ports: 
            - 4000:4000
        depends_on: 
            - mongo
        links: 
            - mongo
            
    mongo:
        container_name: "mongo-prod"
        image: mongo
        restart: always
        ports: 
            - 27080:27080
        volumes: 
            - /opt/production/mongo:/data/db
        env_file: 
            - ./.env
